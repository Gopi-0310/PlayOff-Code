import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.playoffs.app',
  appName: 'Playoffs',
  webDir: 'dist/playoffs',
  bundledWebRuntime: false
};

export default config;
